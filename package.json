{
  "name": "statscore",
  "version": "1.0.0",
  "description": "- You have to refactor code in main.js file. Change it, split it, improve it, do it your own way! - You can use version of node you prefer - Code was tested in node 6, 10, 12, 14, 16, so it should be quite version agnostic at this stage - Application should print out the array of parsed events:",
  "main": "src/main.ts",
  "scripts": {
    "test": "jest",
    "start": "npx ts-node src/app.ts"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^29.1.2",
    "@typescript-eslint/eslint-plugin": "^5.40.0",
    "@typescript-eslint/parser": "^5.40.0",
    "eslint": "^8.25.0",
    "jest": "^29.2.0",
    "ts-jest": "^29.0.3",
    "typescript": "^4.8.4"
  }
}
